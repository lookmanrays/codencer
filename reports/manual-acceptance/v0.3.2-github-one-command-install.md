# v0.3.2 GitHub One-Command Install Acceptance

Date: 2026-07-07

Branch/commit tested for raw installer: `main` /
`3efe9511a9702702e402a315f71211a520342d69`

OS/arch: `darwin/arm64`

## Command Tested

```bash
curl -fsSL https://raw.githubusercontent.com/lookmanrays/codencer/main/scripts/install.sh | sh
```

The command was run with temporary `CODENCER_INSTALL_DIR` and `CODENCER_HOME`
environment values so it did not modify the operator's normal install or home
state.

## Observed Output Summary

- mode: `release-bootstrap`
- selected latest: `v0.3.2`
- platform: `darwin_arm64`
- artifact: `codencer_v0.3.2_darwin_arm64.tar.gz`
- package bin dir: temp unpack directory ending in
  `codencer_v0.3.2_darwin_arm64/bin`
- install_dir: temp `install-bin`
- codencer_home: temp `home`

Explicit assertions:

- did not use caller-cwd `./bin`;
- did not emit `install:bin/codencer`;
- did not require `gh`;
- did not use `sudo`;
- did not edit shell profile files;
- did not require manually pre-downloaded artifacts;
- installed from the downloaded release archive unpack directory.

## Smoke Results

| Command | Expected | Actual | Verdict |
| --- | --- | --- | --- |
| raw latest installer | installs latest release artifact | installed `v0.3.2` `darwin_arm64` in `release-bootstrap` mode | GO |
| pinned installer with `--version v0.3.2 --json` | valid JSON, release-bootstrap, v0.3.2 | valid JSON, `mode=release-bootstrap`, `version=v0.3.2` | GO |
| `codencer version` | reports v0.3.2 | `codencer version v0.3.2` | GO |
| `codencer paths --json` | valid JSON for temp home | valid JSON, temp `CODENCER_HOME` paths | GO |
| `codencer config show --json` | valid JSON config | valid JSON, default `self-host` profile | GO |
| `codencer doctor --json` | valid JSON, no errors | valid JSON, `errors=0`, warnings only for no projects/daemon | GO |
| `codencer setup local --json` | local setup passes or skips service options truthfully | valid JSON, setup passed with service install/start skipped | GO |
| `codencer readiness --json` | valid JSON readiness | valid JSON, `ready_with_skips` for unconfigured live gates | GO |
| patched `install.sh --dry-run --json` with fake network command | no downloads, no extraction, no directory creation | valid JSON plan; temp download/install/home paths were not created | GO |
| source safety child shell | sourcing installer does not close shell | `shell-still-alive` printed | GO |

## Notes

The raw `main` installer still used the pre-polish JSON shape from `v0.3.2`.
This patch adds dry-run `planned_assets` and removes installer Python usage for
manifest verification; those changes are covered by local regression tests and
will take effect after this patch lands.

## Verdict

GO
