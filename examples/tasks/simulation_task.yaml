project_id: "codencer-internal"
run_id: "run-sim-01"
step_id: "step-simulated-workflow"
title: "Simulated Deployment Workflow"
goal: "Verify the orchestrator's state transitions for a multi-step deployment scenario."
context:
  summary: "This is a dummy task used to verify the bridge's handling of success outcomes without calling real LLMs."
allowed_paths:
  - "examples/tasks/simulation_task.yaml"
validations:
  - name: "sim-check"
    command: "echo 'Simulation success'"
acceptance:
  - "Orchestrator transitions the step to 'completed'."
policy_bundle: "default_safe_refactor"
adapter_profile: "codex"
timeout_seconds: 30
is_simulation: true
