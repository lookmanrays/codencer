{
  "mcpServers": {
    "codencer-relay": {
      "type": "http",
      "url": "${CODENCER_RELAY_MCP_URL:-http://127.0.0.1:8090/mcp}",
      "headers": {
        "Authorization": "Bearer ${CODENCER_PLANNER_TOKEN}"
      }
    }
  }
}
